/**
 * File: point.h
 * A class representating a point in N-dimensional space
 * Point is parameterized over an integer N.
 * This allows the compiler to verify the type being used correctly
 */
#ifndef POINT_HEAD__
#define POINT_HEAD__

#include <cmath>
#include <algorithm>

template<std::size_t N>
class Point {
public:
    // Types representating iterators that can traverse and optionally modify the elements of the Point.

};
#endif /* ifndef POINT_HEAD__ */
